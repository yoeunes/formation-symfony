{% extends 'base.html.twig' %}

{% block title %}Article {{ article.title }}{% endblock %}

{% block body %}
<div class="container mx-auto text-gray-700">
    <h1 class="text-4xl mt-12 text-center">{{ article.title }}</h1>
    <div class="mt-8">{{ article.content }}</div>
</div>
{% endblock %}
